# Split vertically
unbind %
bind | split-window -h
# Split horizontally
unbind '"'
bind - split-window -v

# Shift focus
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
